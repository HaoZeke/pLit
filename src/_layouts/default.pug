doctype html
html(lang="{{ page.lang | default: site.lang | default: 'en' }}")

  | {% include head.html %}

  body

    | {% include header.html %}

    main.page-content(aria-label="Content")
        | {{ content }}

  | {% include footer.html %}
  | {% js app %}